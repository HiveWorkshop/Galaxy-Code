//------------------------------------------------------------------------------------------------------------------------------------------------------
//
//  Queue Constructor
//
		int queueCreate(int memoryType);
		void queueDestroy(int queue);
//
//------------------------------------------------------------------------------------------------------------------------------------------------------
//
//  Node Getters
//
		int queueGetNext(int node);
		int queueGetMemoryType(int queue);
		int queueGetPointer(int queue, int node);
		int queueGetPointerBlock(int node);
//
//                            Example
//      ---------------------------------------------------------
//      int node = queueGetNext(queue);
//      int data;
//
//      while (node != -1) {
//          data = memoryInt[queueGetPointerBlock(node)];
//          node = queueGetNext(node);
//
//          //code
//      }
//
//------------------------------------------------------------------------------------------------------------------------------------------------------
//
//  Queue Functions
//
		int queuePush(int queue);
		int queuePushBlock(int queue);
		void queuePop(int queue);
		void queueClear(int queue);
//
//------------------------------------------------------------------------------------------------------------------------------------------------------